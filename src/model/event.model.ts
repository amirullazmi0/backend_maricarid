export class eventRequest {
    id?: string
    name: string
    desc?: string
    images?: JSON
}

export class eventResponse {
    id?: string
    name?: string
    desc?: string
    images?: JSON
}